{
  "name": "SimPad_Settings",
  "version": "0.9.9",
  "main": "app/main.js",
  "package": "electron-packager ./app SimPad_Settings --all --out ./OutApp --version 0.9.0 --overwrite ",
  "dependencies": {
    "h5-color-picker": "^1.0.1",
    "node-hid": "^0.7.2"
  },
  "devDependencies": {
    "bindings": "^1.3.0",
    "electron": "^2.0.1",
    "electron-packager": "^12.0.2",
    "electron-rebuild": "^1.7.3",
    "fs-extra": "^6.0.1"
  },
  "scripts": {
    "postinstall": "electron-rebuild --force -v 2.0.0 --force -m . -w node-hid",
    "start": "electron ."
  }
}
